public class RemoveElement {
    public int[] removeElement(int[] array, int replace){
        for (int i = 0; i < array.length; i++) {
            if (array[i] == replace){
//                array[i] = "_";
            }
        }
        return array;
    }
}
